/**
 * _memset - concatenates two strings
 * @s:  pointer
 * @b: constant byte
 * @n: n bytes of memory
 * Return: s
 */
char *_memcpy(char *dest, char *src, unsigned int n);
{
	while (n)
	{
		s[n - 1] = b;
		n--;
	}

	return (s);
}
