#include "main.h"
/**
 * add -adds to int and returns the result
 * @a: int to be added to b
 * @b: int to be added to a
 * Return: sum od a and b
 */
int add(int, int)
{
	int c;

	c = a + b;

	return (c);
}
